<script>

function setFocus_textbox(elementId)
{
    if (!document.getElementById)
    {
        return;
    }
    
    var eId = document.getElementById(elementId);

    var i = eId.value.replace(/\s/g, "");
    if (i == "")
    {
        i = "";
    }
    var txtBox = eId;
    var txtLength = eId.value.length;

    if (txtBox != null)
    {
        txtBox.focus();
        txtBox.setSelectionRange(txtLength,txtLength);
        txtBox.style.border='1px solid #C42525';
    }
}

function setNoFocus_textbox(elementId)
{
    if (!document.getElementById)
    {
        return;
    }

    var txtBox = document.getElementById(elementId);

    if (txtBox != null)
    {
     txtBox.style.border='1px solid #CCC';
    }
}

$(function () {

  c = "flag_recently_viewed";
  i = $.cookie("items_recently_viewed");
  if ($.cookie(c) != "yes" && i)
    {
        var cook = $.cookie("items_recently_viewed");
        
        $.get('/viewed/'+ cook, function(data){
          $("#right-panel > #viewed").hide().html(data).fadeIn(1000);
        });
    }
    
});

</script>
<script type="text/javascript">

  var _gaq = _gaq || [];
  _gaq.push(['_setAccount', 'UA-32882383-1']);
  _gaq.push(['_trackPageview']);

  (function() {
    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
  })();

</script>

<script type="text/javascript">
$(function() {
    $(window).scroll(function() {
        if($(this).scrollTop() != 0) {
            $('#toTop').fadeIn();    
        } else {
            $('#toTop').fadeOut();
        }
    });
 
    $('#toTop').click(function() {
        $('body,html').animate({scrollTop:0},'slow');
    });    
});
</script>
