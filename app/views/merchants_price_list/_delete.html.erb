<%= form_for @price_list_product, :url => {:controller => 'merchants_price_list', :action => 'destroy'},:html => {:id => "price-list-delete-form"} do |f| %>

    <%= hidden_field_tag 'part1_product_name' , params[:part1_product_name] %>
    <%= hidden_field_tag 'merchant_action', params[:merchant_action] %>

<% end %>

<span class="delete-text"><%= "Do you want to place a request to delete this product?" %></span>

